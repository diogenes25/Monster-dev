# knowledge — `impl-10`

Stack: `dom-css`

Raw material. Nothing here is published, and nothing here reaches
`stacks/dom-css/README.md` without passing its gate. Written down because it was observed, not
because it is known to help.

Source: run [[2026-08-01-index-sonnet]], Sonnet, no-change arm.

**Captured `2026-08-02` by the `#012` backfill, and not yet narrated.** `step-1-fixture/` and
`step-4-result/` are the real thing, byte for byte: the single commit the run started from, and
the worktree as it was handed back. `step-2-requirement/target-wish.md` is the brief verbatim out
of the transcript. What is **missing** is `step-2-requirement/dialog-NN.md` and the whole of
`step-3-process/` — those are read off the transcript by a person, and this line is here so this
folder is not mistaken for a finished record.

## What is visible without opening the transcript

Built after §2 became a table with a first-match rule. The only implementation that parameterised the frame count instead of hard-coding it.

- Changed `script.js`, `style.css`, plus the sprite at `assets/monster.png`.
- Wrote the monster element **from JavaScript, leaving the markup untouched**.
- Named the sprite `monster.png`. Three different names appear across the ten implementations.
- Fetched `green-fuzz-classic`, like every other implementation on record.

## Open

Everything. No observation from this folder has been promoted, and one implementation is not a
signal — see `../knowledge.md` for what is being watched across the surface.
