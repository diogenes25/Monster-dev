---
type: run
title: Run ph0-smoke
description: A harness smoke test, not an implementation. Its brief was to answer with the word OK and change no files, and its run folder no longer exists.
resource: ph0-smoke
tags: [smoke-test, sonnet, harness]
timestamp: 2026-08-01
---

# Run `ph0-smoke`

A harness smoke test, not an implementation. Its brief was to answer with the word OK and change no files, and its run folder no longer exists.

No implementation and no `impl-NN/` folder: nothing was built. This folder holds the
transcript and nothing else, which is exactly the case the run record exists to be able to
hold — a run is not the same thing as an implementation.

*Captured `2026-08-02` by the `#012` backfill, from the rescue copy taken the same day, not by
`hire.ps1`. Everything above the prose is Open Knowledge Format; the tags are the only place
this run's role in the series is written down.*
