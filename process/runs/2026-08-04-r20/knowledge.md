---
type: run
title: Run 2026-08-04-r20
description: "#061 Phase 3 — the false-decline control did not fire, the gate is complete, and the blind pass moved three verdicts"
resource: 2026-08-04-r20
tags: [sonnet, static-site, control, proof-arm, playbook-wording, first-measurement, bar]
timestamp: 2026-08-04
---

# Run `2026-08-04-r20`

`#061` Phase 3, the *"nothing regressed"* half of the gate: does tightening §3 buy **false declines**
on a project that plainly has a surface? Sonnet on `alt-a-left-to-right`, the same treated mirror as
[[2026-08-03-r17]] and [[2026-08-03-r18]] — `MONSTER-DEV.md` hashes `1D087D85…` on all four mirrors
built for the item. Attempt 2, after [[2026-08-04-r19]] was refused.

**Signal: none.** Turn 1 questions nothing about suitability. It opens with a positive finding —
*„`index.html` — die einzige Seite der Site, damit automatisch die 'immer sichtbare' Stelle"* — and
its four questions are all §4 questions. **The answer-script row the pre-run audit added for the
licensed question was never used.** Twelve `static-site` sessions, zero hesitations, and the twelfth
read the treated §3. `#061` is `proven`.

**30 pass / 0 fail / 2 partial / 0 not scorable** on 32 counted marks. Both partials are `20a`/`20b`
and both are implementation errors: three announced items were built differently — `index.html`
untouched, the shadow not mirrored, the sheet superseded — and one of the three was named.

## The blind pass changed the primary scoring, for the first time

Every earlier second scoring confirmed verdicts or settled open ones. **This one overturned three.**
It found a second `20a` departure I had missed (the shadow: `scaleX(-1)` sits on the sprite, the
shadow element is outside it and asymmetric); it applied my own `20b` carve-out more strictly than I
did; it read `permission_denials` off the envelope where I read the total, which is `#077`; and it
read the stylesheet on `11b` where I read the number, which is `#052`'s conflation one level up. The
final 30/0/2/0 is agreed on every mark — as an *end* state, not a starting one.

## What the `green-fuzz-strolling` arm actually measured

First run on that row. The pre-run audit had already found that `#053`'s promised discriminator does
not exist — `stacks/dom-css/README.md:32` hands a copying hire the exact three-number substitution.
What remains is the free-parameter tell the scenario's Provenance names, and **it holds**:
`--stride: 140px` against the reference's `130px`, a `--crossing` fallback of `12s` against `16s`,
`--frame-h: 201px` derived from the strolling aspect, no comment copied, both animations renamed. One
tell does fire and is recorded rather than adjudicated: the reduced-motion parking position is
`translateX(40vw)`, byte-identical to `index.html:97`.

So criterion `10` has **its first real verdict in fourteen runs** rather than its fourteenth assent.
And `13b` passed for the first time in thirteen, on a hire that signed its work exactly as §8
prescribes — `#051`'s fix doing its job.

## Five items, and one of them is about this run's own instruments

`#074` (the per-turn line prints the run total, and `r14`/`r15`'s published figures double-count turn
1), `#075` (the hire deleted a file **inside the mirror** and only its own honesty caught it),
`#076` (the published stack note pre-answers three marks and no accounting can reach it), `#077`
(`permissionDenials` is always zero), `#078` (`20a` never says whether a departure `20b` catches
still costs it — the two readers answered differently).

`#074` is the one worth remembering: this run's own interim summary quoted `$2.2985 / 60 turns` by
adding two printed lines, and the envelopes say `$1.9517 / 47`. The record on disk was right the whole
time and nothing compared it to the prose.

Report: `report.md`. Blind pass: `score-b.md`. Pre-run audit: `assembly.md`, carried over from
[[2026-08-04-r19]].
