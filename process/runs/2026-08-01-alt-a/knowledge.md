---
type: run
title: Run 2026-08-01-alt-a
description: The first run. Established the criteria the whole series is scored against, and found the §4 ordering failure.
resource: 2026-08-01-alt-a
tags: [first-measurement, opus, dom-css]
timestamp: 2026-08-01
---

# Run `2026-08-01-alt-a`

The first run. Established the criteria the whole series is scored against, and found the §4 ordering failure.

The implementation it produced is [[impl-02]] in the record tree. The verdict is in
`report.md` beside this file where there is one; the transcript is the only record of *how*
the hire worked and is the one artefact here that could not be reconstructed.

**Not a clean-room run, established `2026-08-03` and recorded in `#042`.** Turn 1's entry-point path
ran through a session scratchpad whose slug spells out this repository's absolute location, and the
hire followed it: two `git remote -v` calls, a listing of the repository root naming `CLAUDE.md`, and
the sprite sheet copied straight out of the working copy. Nothing shows it read the criteria, and the
§4 ordering finding stands — but the isolation section of `report.md` describes a control that was
not in place, and it now says so. Cite this run with that caveat.

*Captured `2026-08-02` by the `#012` backfill, from the rescue copy taken the same day, not by
`hire.ps1`. Everything above the prose is Open Knowledge Format; the tags are the only place
this run's role in the series is written down.*
