---
type: run
title: Run 2026-08-04-r19
description: "#061 Phase 3, attempt 1 — no data: the entry point was refused, and the refusal narrowed #050"
resource: 2026-08-04-r19
tags: [sonnet, static-site, entry-point, no-data, control]
timestamp: 2026-08-04
---

# Run `2026-08-04-r19`

`#061` Phase 3, first attempt. Sonnet, `static-site`, treated mirror. **The hire read `START.md`,
declined to fetch the playbook and stopped in turn 1** — the second instance of `#050` after
[[2026-08-03-r13]]. `$0.1356`, 2 model turns, worktree clean, **no criterion reached**, including the
false-decline observation the phase exists for: a hire that never read §3 cannot have been made
hesitant by it. Rerun unchanged as [[2026-08-04-r20]].

**The refusal carried a finding, and it is about the harness.** Both refusals on record lead with the
same sentence *before* the supply-chain argument: §0 tells the hire to derive the base URL from the
URL it was fetched from, and on a mirror run the entry point arrives as a filesystem path, so there
is no URL and *„ich würde eine raten müssen, und das mache ich grundsätzlich nicht."* Both are Sonnet
on `static-site`; both offer the identical alternative, to build without the playbook.
[[2026-08-01-live]], the one run over real URLs where §0 resolves, accepted — but it is Opus.

That **narrows `#050`'s proof design from a fleet to one arm**: a Sonnet real-URL run against
`static-site`. If the refusal is about §0 having no referent it should not fire there; if it fires
anyway, the *within-tier variance* reading survives and the expensive design was right. Not a finding
against §0, which is `proven` — what failed is a run class in which §0 has nothing to point at.

**The pre-run audit was not wasted and is this run's other output.** Eight findings, two of which
changed the scenario before any turn was bought: an answer-script row for the question the treatment
authorises, without which the control could only ever have returned *no hesitation*; and a `20a`
carve-out, because criterion `19` makes a departure from the announced sheet structurally unavoidable
on a stated-monster arm. Both survive into [[2026-08-04-r20]] unchanged, which is why that run's
`assembly.md` cites this one rather than repeating it.

Report: `report.md`.
