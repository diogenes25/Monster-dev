---
type: run
title: Run 2026-08-03-r16
description: Opus on the decline scenario — settled #043's attribution as a §3 playbook gap, and rejected #046
resource: 2026-08-03-r16
tags: [opus, nowhere-to-walk, decline, attribution, second-sighting, playbook-wording]
timestamp: 2026-08-03
---

# Run `2026-08-03-r16`

The second model on the decline scenario, bought to give `#043` and `#046` a second sighting off one
transcript. Sibling of [[2026-08-03-r12]], which is the Sonnet arm; nothing here is comparable with
the `static-site` series and none of it may be pooled with [[2026-08-03-r15]] or [[2026-08-03-r14]].

**Two opposite answers from one run, which is why it was worth buying as a pair.**

`#043` is **settled as a §3 playbook gap.** Opus did what Sonnet did and leaned harder: found the
absence unaided, refused ASCII art by argument, changed nothing — then recommended building an HTML
report and asked permission. Half C wants *every* model doing the same before a gap is called, and
that is now two models on two tiers with four independent scoring passes. §3 says what to **do** and
names one thing not to do; neither reading forbids *offering* the workaround as a question.

`#046` is **rejected.** Its instrument is the read order, and Opus read the project third — turn 1
touched only `START.md`, `MONSTER-DEV.md` and one directory listing before looking at `report.py`.
Every one of the seven playbook reads that made up `r12`'s ordering is absent. The `stacks/dom-css`
fetch that fails criterion `10` happens in **turn 2**, after the decision to build, for which
`dom-css` is the correct row. So §2's ordering dependency is inferable as written, and `r12`'s
ordering was one model's habit.

**The thing no earlier run produced: it built the whole feature after being told there was nowhere to
build it.** Turn 2 read the deflection row — *„Du bist der Fachmann — sag mir, was Sinn ergibt."* —
as consent, where `r12` read the identical sentence as a decline. 35 model turns and `$2.43` for an
`--html` path in `report.py`, the sheet copied to the project root, and a verified walking monster.
Careful work throughout: stdout diffed byte-identical against `HEAD`, opt-in flag, two viewport
widths and `prefers-reduced-motion` checked in headless Chromium, scratch harness outside the project
and deleted, nothing committed. A hire that followed all of the playbook except the part that says
stop. That is what makes criteria `6`, `8` and `10` fail **downstream of** `4` and `7` rather than as
findings of their own.

**8 pass / 5 fail / 0 partial / 1 not scorable** against `r12`'s 10 / 3 / 0 / 1 on the same fourteen
counted marks. Full scoring and the resolved forks: `report.md`; blind pass: `score-b.md`; what the
setup was audited for before it ran, including one defect fixed and one deliberately not:
`assembly.md`.

Four items came out of it — `#057` (the runs-root path names the harness to every hire), `#058`
(criterion `12`'s `NOT SCORABLE` carve-out survives its own premise failing), `#059` (a bundle note
that asserted a fact this run falsified), `#060` (the deflection row read as consent by one model and
as a decline by another) — plus an evidence line on `#050`, whose denominator is now 13 sessions and
1 refusal.
