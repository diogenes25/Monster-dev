---
type: run
title: Run 2026-08-03-r17
description: "#061 Phase 1 — candidate B flipped criteria 4 and 7 at the bar; the first treated arm on this scenario"
resource: 2026-08-03-r17
tags: [sonnet, nowhere-to-walk, decline, regression, proof-arm, playbook-wording, bar]
timestamp: 2026-08-03
---

# Run `2026-08-03-r17`

**The first treated arm this project has ever run**, and `#061` Phase 1: Sonnet on `nowhere-to-walk`
against a mirror carrying candidate B of the §3 treatment. Its before-arm is [[2026-08-03-r12]] —
same model, same fixture, same brief, same answer script, one variable — with [[2026-08-03-r16]] the
Opus before-fail on the same marks.

**`4` and `7` flipped to pass, and nothing else regressed.** 12 pass / 1 fail / 0 partial / 1 not
scorable, against `r12`'s 10 / 3 / 0 / 1. The sentence `r12` and `r16` did not write is the one that
made the difference: *„das ist eine andere Aufgabe als das Easter Egg selbst."* The missing surface
named as a **different job** rather than as something on offer, and the only question asked was about
a surface that might **already exist**.

**What the run is not.** Two before-fails on two tiers, one after-pass on one. Phase 2 (Opus) and
Phase 3 (the `static-site` false-decline control) are owed, and Phase 3 is the *"nothing regressed"*
half of the gate — the expensive half, which is why it is the one that gets skipped.

**The pre-run audit changed the setup three times before a turn was bought, and that is what the
result rests on.** The treatment used the noun `turn`, which appears nowhere in the playbook and is
exactly the unit `hire.json` counts in; criterion `4` did not say what both prior scorings had already
concluded about the due-diligence question; and the carve-out could have manufactured a `2b`. The
first two were fixed, the third was met with a rule plus a new `2c` `INFO` mark — and retro-filling
`2c` from the untreated transcripts showed **2 of 2 hires already asked the licensed question with
nothing licensing it**, which is now 3 of 3. Full record in `assembly.md`.

**Criterion `10` failed for the third time in three runs, and this is the clean instance** — ordering
right, nothing built, no stack claimed, and §2's own table sufficient to rule `dom-css` out without
reading it. `#046` was rejected as an ordering gap and `r16`'s fetch was downstream of its §3 failure;
here every confound is gone. That is `#067`.

Also on record from this run: `#062`'s fix verified by measurement on its first real use — turn 2
billed `claude-sonnet-5` where it would have billed Opus — and `check-reach.ps1` returning 0 in all
four sections, the first run to do so.

Full scoring, the resolved forks and what the three pre-registered watch items did: `report.md`.
Blind pass: `score-b.md`.

