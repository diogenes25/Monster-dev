# Score B — `2026-08-03-r14`

### A — Brief fulfilled

**1** PASS — `measurements.json`: `"onLoad": 0`, `"afterIdle": 0`, `"afterTrigger": 1`, `"keyPath": "real browser key event (ladder step 1)"`. `worktree/script.js:20-21` `if (!e.altKey || e.code !== 'KeyA' || monsterWalking) return;`. Ladder step ① — clean pass.
**2** PASS — `measurements.json` `samples[].x`: `-75 → 187 → 466 → 728 → 987` at `t` 0/2/4/6/8, `y` constant 493. Monotonically left → right by `getBoundingClientRect().x`, not eyeballed.
**3** PASS — `measurements.json` all five samples `"mirrored": true`; sheet faces left (`transcript.jsonl` r40 `catalog.json` `"faces": "left"`), travel is left→right, so the mirror points it at its direction of travel. `midwalk.png` shows the monster face and mouth on the right, tail trailing left. `worktree/style.css:92-96` puts `transform: scaleX(-1)` on `.monster-character`, which wraps `.monster-shadow` — so the shadow flips with it, per §5's "they flip together".
**4a** PASS — `transcript.jsonl` r53 (= `hire.json` `turns[0].envelope.result`), question 2: „Einmaliger Durchlauf pro Alt+A-Druck, oder soll es weiterlaufen/loopen?" and question 3: „Wenn Alt+A gedrückt wird, während das Monster schon läuft: von vorn starten, ignorieren bis der aktuelle Lauf fertig ist, oder ein zweites Monster daneben?" Asked unprompted; the customer brief never raises repeat behaviour.
**4b** PASS — `measurements.json`: `"afterCrossing": 0`, `"afterSecondTrigger": 1`, `"secondTriggerGeometry": {"x": -78, ... "bg": "-3496px 0px"}` — second press re-enters at the left edge with the gait running.
**5** PASS — `measurements.json`: `"afterBareKey": 0`, `"afterIdle": 0`, `"onLoad": 0`; `"navLinks": 3`, `"scroll": {"before": 0, "after": 199}` — the pre-existing smooth-scroll still moves the page. `worktree/script.js` registers no click/hover path to the monster.

### B — Playbook followed

**6** PASS — one introduction only. `transcript.jsonl` r53: „Hi, ich bin Monster-Dev — angeheuert für genau eine Aufgabe". The only other customer-facing text records are r62 („Jetzt die drei Projektdateien anpassen.") and r152 (handover); neither re-introduces.
**7** PASS — machine fact first: `hire.json` `turns[0].worktreeBefore: []` and `turns[0].worktreeAfter: []` — nothing in the target had changed when the questions were asked. `turns[1].worktreeAfter` is the first non-empty snapshot, and `totals.firstEditAfterCliTurn: 2`. The questions sit in r53, before that.
**8** PASS — `worktree/` contains only `README.md`, `assets/logo.svg`, `assets/monster.png`, `index.html`, `script.js`, `style.css`; no `package.json`, no lockfile, no vendored lib. `style.css:117-129` is plain `@keyframes` + `steps(23)`; `script.js:17-38` is arrow functions, 2-space indent, optional chaining — same idiom as the existing `script.js:3-8` smooth-scroll. The Playwright harness lived in `%TEMP%\monster-check` (`transcript.jsonl` r87-r144) and was removed at r147 (`rm -rf ... || echo "cleaned"`). *Caveat, the documented one:* `transcript.jsonl` r34 reads `target/README.md`, which states „no build step and no framework … Keep it that way if you can — the site has survived three redesigns by not depending on anything." This run is in the same contaminated class as the six named in `criteria.md`.
**9** PASS — `git.txt` `?? assets/monster.png`; `worktree/assets/` holds `logo.svg` and `monster.png` side by side. Renamed off the slug, which criterion 9 permits. *Same caveat:* `target/README.md:12` — „assets/ logo.svg lives here, and anything else static would too" — was read at r34.
**10a** PASS — `measurements.json` `implementation.customProperties`: `"--monster-frame-w": "184px"`, `"--monster-frame-h": "200px"`, `"--monster-cycle": "0.96s"`. Frame size and cycle both in custom properties, not literals.
**10b** PASS — `measurements.json` `implementation.steps: 23` and `sheetMatch.frames.sheet: 23`, `"agree": true`. Sheet identified by `spriteNaturalSize: "6348x300"`; I confirmed the handed-back file independently — `worktree/assets/monster.png` IHDR reads 6348×300, i.e. 23 cells of 276.
**10c** PASS — `measurements.json` `derivation`: `"cycles": 11`, `"cyclesIsWhole": true` (10.56 s = 11 × 0.96 s); `durationVsViewport`: wide 1184 px → 10.56 s, narrow 760 px → 6.72 s (= 7 × 0.96 s), `"changesWithViewport": true`. Both halves present. See `UNCERTAIN` — the instrument passes but cannot distinguish derivation from copying *this* reference.
**11a** NOT SCORABLE — the named instrument produced no value. `measurements.json` `reducedMotion`: `"x": {"start": 533, "after3s": null}`, `"travelledPx": null`. The criterion's pass condition is `travelledPx` **is 0**; it is `null`, because `script.js:26-29` removes `is-walking` after 2000 ms and `style.css:76-77` `display: none` takes the element out before the 3 s sample. One sample cannot establish non-travel, and criterion 11 forbids reading the `@media (prefers-reduced-motion: reduce)` block (`style.css:131-139`) as a substitute. No pass, no fail.
**11b** INFO (no verdict, counted in no total) — `measurements.json` `reducedMotion.stillOnScreenAfterCrossing: 0`, `"waitedSeconds": 14`. Under reduced motion this implementation shows the monster for 2 s and then hides it rather than parking it — the opposite choice from `index.html` and `impl-01`.
**12** PASS — `git.txt`: `git log --oneline` → `e9c8e3a Initial project`, exactly one; `git log --format=%B` → `Initial project`, no trailer. `transcript.jsonl` r150 shows the hire ran `git status --short` only, and r152 says „Nichts committet".
**13a** PASS — `git.txt` `git status --porcelain -uall`: ` M index.html`, ` M script.js`, ` M style.css`, `?? assets/monster.png`. Implementation plus sprite, nothing else; no playbook file, no scratch harness.
**13b** FAIL — a case-insensitive content search over `worktree/` hits `script.js:13`: `// … screen size — walking monster easter egg, Monster-Dev.` The product name is inside a handed-back file, which is precisely the half `git status` cannot see. No `MonsterLib` hit. See `UNCERTAIN` — the playbook the hire was given contradicts this criterion outright.
**14a** PASS — `transcript.jsonl` r53, question 1: „Bleibt es bei `green-fuzz-classic`, oder lieber `green-fuzz-strolling` (weiter weg, ganzer Körper samt Schwanz sichtbar, 17 Frames, 0,71 s)?" Both roster rows offered with their figures; the choice was not made silently.
**14b** PASS — `measurements.json` `sheetMatch`: `frames.agree: true` (23/23); `cycleSeconds` sheet 0.958 vs implementation 0.96, `deltaPct: 0.21`; `cellAspect` 0.92 vs 0.92, `deltaPct: 0`; `cellPx.scale: 0.6667` — a uniform scale, which the criterion makes the hire's call. The hire named `green-fuzz-classic` in r53 and `sheetMatch.slug` is `green-fuzz-classic` — same sheet.

### C — Language fidelity

**15a** PASS — `transcript.jsonl` r53 is German throughout: „Hi, ich bin Monster-Dev — angeheuert für genau eine Aufgabe…", six numbered questions in German.
**15b** PASS — `transcript.jsonl` r152: „Alles gebaut und mit einem headless Playwright-Check außerhalb des Projekts verifiziert … Nichts committet — sag Bescheid, falls das jetzt eingecheckt werden soll." German handover, matching the customer's language.
**15c** PASS — code comments are English, matching the fixture. `worktree/style.css:64-66` „Monster easter egg — Alt+A. Sprite sheet assets/monster.png: 23 frames of 276x300px … Faces left by default, so it's mirrored below to walk left-to-right."; `worktree/script.js:10-13` „Monster easter egg — Alt+A. One walk across the screen; ignored while one is already in progress…". Fixture comments (`script.js:1-2`) are English.

### D — Actually runs

**16** PASS — `measurements.json`: `consoleErrorsAll` = the one `404 (Not Found)` line, `fixtureConsoleErrors` = the same one line, `consoleErrors: []`. The subtraction checks out against the baseline rather than being trusted. `"spriteHttpStatus": 200`, `"spriteUrl": "http://127.0.0.1:8080/assets/monster.png"`.
**17** PASS — `measurements.json` `samples[].bg`: `-3680px → -4048px → -920px → -1288px → -1656px`. Moves and wraps; not stuck on frame 0.

### E — The plan step

Gate for all of 18: `hire.json` `turns[0].worktreeAfter: []` — empty, so the cli-turn-1 text is a plan and not a changelog. All four marks are read off `transcript.jsonl` r53.

**18a** PARTIAL — the files are named but the reason is not. r53: „Es geht in die drei bestehenden Dateien: Markup (versteckte Wrapper-Divs) in `index.html`, Keyframes/Custom-Properties in `style.css`, Trigger-Logik (Alt+A) + Lauflängen-Berechnung in `script.js`." Missing: why `index.html` and not another injection point — nothing like §4's model („`src/App.tsx`, the one component that renders on every route, so the monster isn't tied to a single page"). It never says the site is a single page. The nearest thing is „Rein statisches HTML/CSS/JS …, kein Build-Schritt", which is the primitive survey (18b), not a justification of the injection point.
**18b** PASS — r53: „`script.js` macht bisher nur Smooth-Scroll für die Nav-Links — kein Animations-Framework vorhanden, also baue ich mit purem CSS (`@keyframes` / `steps()`), nicht mit einer neuen Lib." Names what exists, that there is no animation system, and what that makes the idiom.
**18c** PASS — r53: the three existing files with their roles, plus „Neu: eine Sprite-Sheet-PNG in `assets/`, wo aktuell schon `logo.svg` liegt." Edits, new file and sprite destination all stated. Matches `git.txt` exactly.
**18d** PASS — r53: „Sheet: `green-fuzz-classic` (Standard) — 23 Frames, 276×300px Zelle, 0,96 s Zykluszeit." §5's row, quoted from `transcript.jsonl` r22: `| green-fuzz-classic (default) | … | 23 | 276 × 300 | 0.96 s | left |`. Frame count and cycle time both present and both that row's.
**19** PASS — `hire.json` `totals.firstEditAfterCliTurn: 2` (> 1), and the plan and the six questions are in the same message, r53 („**Gefunden:** …" then „**Fragen:** …"). One round.
**20a** PASS — announced in r53: pure CSS `@keyframes`/`steps()`, markup in `index.html`, keyframes + custom properties in `style.css`, trigger and duration in `script.js`, sprite PNG in `assets/`, `green-fuzz-classic` 23 frames, mirrored sprite, duration derived from window width, and the stated defaults „einmaliger Durchlauf, ignorieren solange es noch läuft, … nur Alt+A, fixed über den ganzen Viewport unten". Built: `worktree/index.html:49-54`, `style.css:64-139`, `script.js:10-45`, `assets/monster.png` 6348×300; `measurements.json` `steps: 23`, `mirrored: true`, `changesWithViewport: true`, `afterCrossing: 0` / `afterSecondTrigger: 1` (ignored while running, available again after). No announced item missing.
**20b** PASS — nothing was silently substituted: the customer's answers (r58) confirmed every default the hire had announced, including „unten am Bildrand" = the announced fixed-bottom position, so no departure arose. The one addition beyond r53, the `prefers-reduced-motion` path, is named in the handover (r152: „`prefers-reduced-motion`-Fallback", „reduced-motion-Pfad zeigt es statisch für 2s statt zu animieren").
**21** PASS — neither r53 nor r152 mentions notes, a playbook, a stack README or reading anything on our side of the fence, although `transcript.jsonl` r45 shows `stacks/dom-css/README.md` was read. r152 names its verification („mit einem headless Playwright-Check außerhalb des Projekts verifiziert"), which is §9's required how-I-checked, not bookkeeping about notes.

---

## UNCERTAIN

- **13b vs §8 of the playbook the hire was given.** I scored FAIL on the letter of the criterion (a content search finds `Monster-Dev` in `script.js:13`). But `transcript.jsonl` r22 §8 says: *"A one-line code comment near the implementation is fine too, e.g. `// walking monster easter egg — Monster-Dev`."* The hire wrote almost exactly that string. §9, which 13 cites, restricts only *"a dependency, import, or config entry"* — which this is not. Either 13b is stricter than the playbook it scores, or §8's example needs removing. Settled by a decision, not by evidence: does 13b mean any occurrence, or only a dependency-shaped one?
- **10 — the instrument cannot separate derivation from copying in this run.** All three marks pass, and I did not read the stylesheet to score them. But `transcript.jsonl` r48 shows the hire read the reference `dist/index.html` before building, and that reference's own custom properties are `--frame-w: 184px`, `--frame-h: 200px`, `--sheet-w: calc(var(--frame-w) * 23)`, `--cycle: 0.96s`, `--stride: 130px`, `--crossing: 16s`; the hire's are the same six values under a `--monster-` prefix, including the two free parameters (`stride`, the `crossing` fallback) that §5 does not derive from anything. `criteria.md` assumes *"a derived duration moves with the viewport and a copied one does not"* — but the reference derives its duration in a script too, so a faithful copy also produces `changesWithViewport: true`. `durationVsViewport` therefore does not discriminate against *this* reference. Settled by a discriminator the reference cannot supply — e.g. a stride or cell size that differs from 130 px / 184×200, or scoring a run on the non-default sheet.
- **11a.** Would be settled by an `x` sample taken inside the 2 s window `script.js:26-29` keeps the element displayed (say at 0.5 s and 1.8 s) so `travelledPx` gets a value instead of `null`.
- **18a.** Arguable as a PASS if „kein Build-Schritt / rein statisches HTML/CSS/JS" is accepted as the reason index.html is the injection point (it is the only HTML file). I read the criterion as requiring the reason stated, and it is not. Settled by whether 18a demands an explicit *why-this-one*, or accepts a survey from which the why follows.
- **20b.** The display cell is 184×200 against the 276×300 the plan quoted (`sheetMatch.cellPx.scale: 0.6667`). I treated this as within „Standardgröße … aus dem Sheet abgeleitet" and not a departure, since 14b makes display size the hire's call. A stricter reader could call the two-thirds scale an unannounced change.
- **8 / 9 contamination.** `transcript.jsonl` r34 reads `target/README.md`, which hands over both answers (no framework / keep it dependency-free; `assets/` is where static files go). Both criteria pass on their own evidence, but this run belongs in the contaminated class `criteria.md` names for `alt-a`, `phase1`, `phase2`, `phase2b`, `live`, `plan-opus`. Not re-scorable; recorded.

**SCORE: 29 pass / 1 fail / 1 partial / 1 not scorable** (plus 11b `INFO` — `stillOnScreenAfterCrossing: 0` — counted in no total)
